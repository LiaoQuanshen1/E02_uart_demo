# MM32F327X_G8P 程序开发指南
> **目标：** 安全、高效地基于逐飞科技开源库进行嵌入式开发。
---
## 一、不可修改的文件

以下文件/文件夹保存了**关键项目信息**，除非用户**明确要求**修改，否则禁止改动：

| 路径 | 内容 | 重要性 |
|------|------|--------|
| `*.md` 文件 | 库函数使用说明、依赖关系、版本信息 | 开发参考 |
| `*.txt` 文件 | 引脚分配、文件夹说明等 | 硬件配置依据 |
| `libraries/` 文件夹 | 所有库函数源码（SDK + Common + Driver + Device + Components） | 核心驱动库 |

> ?? **如果 AI 的判断与上述文件中的内容冲突，AI 必须立即向用户指出矛盾之处。**

---

## 二、API 调用原则

### 2.1 优先调用现有接口

`libraries/` 包含完整的分层驱动库，**添加新功能时优先使用已有 API**，不得自行重新造轮子。

### 2.2 优先调用上层函数

调用优先级：**上层 > 下层**，尽量不与底层直接关联。

```
  ⑤ 应用组件层 (components/)          ← 最优先
  ④ 外接设备驱动层 (zf_device/)       ← 其次
  ③ 芯片外设驱动层 (zf_driver/)       ← 再次
  ② 开源库公共层 (zf_common/)         ← 辅助
  ① 芯片 SDK 底层 (sdk/)             ← 最后选择（一般不直接调用）
```

### 2.3 入口文件

只需包含 `#include "zf_common_headfile.h"` 即可使用全部 API。各层具体 API 详见 `libraries/` 内对应头文件。

---

## 三、用户代码规范

- 用户代码放在 `code/` 文件夹下（**不要创建子文件夹**，直接放文件）
- 所有 HAL 层（`sdk/drivers/hal_*`）寄存器操作已有上层封装，**禁止直接操作寄存器**
- 添加新外设时，先检查 `zf_driver/` 和 `zf_device/` 中是否已有现成驱动
- **引脚分配**详见 `MM32F327X_G8P智能车推荐引脚分配.txt`，添加新功能前必须检查引脚是否已被占用
- **库依赖关系**详见 `库文件依赖关系与使用说明.md`

---

## 四、冲突处理

当 AI 的判断与 `libraries/` 内文档或引脚分配文件不一致时，**AI 必须**：

1. 立即指出冲突内容
2. 引用相关文档原文作为依据
3. 等待用户确认后再执行
