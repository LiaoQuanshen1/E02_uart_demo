# MM32F327X_G8P 逐飞科技开源库 — 文件依赖关系与使用说明

> **适用平台：** MM32F327X_G8P  

---

## 一、整体架构总览

本开源库采用**分层架构**设计，共分为五层，自底向上依赖：

```
┌─────────────────────────────────────────────────────────────┐
│  ⑤ 应用组件层 (components/)                                  │
│     FatFs 文件系统                                           │
├─────────────────────────────────────────────────────────────┤
│  ④ 外接设备驱动层 (zf_device/)                               │
│     摄像头 / 显示屏 / IMU / 电机编码器 / GPS / SD卡 / 蓝牙等    │
├─────────────────────────────────────────────────────────────┤
│  ③ 芯片外设驱动层 (zf_driver/)                                │
│     GPIO / UART / SPI / IIC / ADC / PWM / 定时器 / DMA 等     │
├─────────────────────────────────────────────────────────────┤
│  ② 开源库公共层 (zf_common/)                                  │
│     类型定义 / 时钟 / FIFO / 中断 / 工具函数 / 字体 / 调试      │
├─────────────────────────────────────────────────────────────┤
│  ① 芯片 SDK 底层 (sdk/)                                       │
│     CMSIS / HAL Drivers / 寄存器定义 / 启动文件                 │
└─────────────────────────────────────────────────────────────┘
```

**核心入口文件：** `zf_common/zf_common_headfile.h`  
该文件已包含所有层的头文件，用户工程中只需包含此一个头文件即可使用全部 API。

---

## 二、各层文件依赖关系详解

### 2.1 第①层 — 芯片 SDK 底层 (`sdk/`)

| 文件 | 功能 | 依赖 |
|------|------|------|
| `sdk/system_mm32f3277g.c` | 系统初始化、时钟配置 | `system_mm32f3277g.h` |
| `sdk/system_mm32f3277g.h` | 系统初始化函数声明 | 无 |
| `sdk/mm32f3277g.h` | 主芯片头文件，包含所有外设地址定义 | 无 |
| `sdk/mm32f3277g_features.h` | 芯片特性定义 | 无 |
| `sdk/hal_device_registers.h` | 外设寄存器地址映射 | 无 |
| `sdk/CMSIS/` | ARM CMSIS 内核头文件（core_cm4.h 等） | 无 |
| `sdk/drivers/hal_*.c/h` | 各外设 HAL 驱动层（ADC/GPIO/UART/SPI等） | `hal_device_registers.h` |
| `sdk/drivers/hal_common.h` | HAL 通用定义 | `hal_device_registers.h` |
| `sdk/iar/startup_mm32f3277g.s` | IAR 启动文件 | 无 |
| `sdk/iar/linker/` | IAR 链接脚本 | 无 |
| `sdk/mdk/startup_mm32f3277g.s` | MDK 启动文件 | 无 |
| `sdk/mdk/linker/` | MDK 链接脚本 | 无 |

> **说明：** 本层是官方 SDK，逐飞科技在此基础上进行封装。用户一般**无需直接调用** HAL 层的 API，而是通过上层的 `zf_driver_*` 接口操作。

---

### 2.2 第②层 — 开源库公共层 (`zf_common/`)

| 文件 | 功能 | 依赖 |
|------|------|------|
| **`zf_common_typedef.h`** | ? **核心基础头文件** — 自定义类型（uint8/uint16/uint32 等）、布尔宏、启用禁用宏 | `stdio.h`, `stdint.h`, `stdbool.h`, `stdarg.h`, `string.h`, `stdlib.h` |
| **`zf_common_headfile.h`** | ? **一键包含头文件** — 包含 SDK 所有 HAL、common 层、driver 层、device 层全部头文件 | 本层及以下所有模块 |
| `zf_common_clock.h` | 系统时钟配置枚举与全局时钟变量声明 | `zf_common_typedef.h` |
| `zf_common_clock.c` | 时钟初始化实现（PLL 锁相环配置） | `zf_common_clock.h`, `zf_common_function.h`, `zf_common_interrupt.h`（间接通过 `hal_device_registers.h` 操作 RCC 寄存器） |
| `zf_common_fifo.h` | FIFO 环形缓冲区结构体与 API 声明 | `zf_common_typedef.h` |
| `zf_common_fifo.c` | FIFO 读写/清空/状态查询实现 | `zf_common_fifo.h` |
| `zf_common_font.h` | 字库（6x8、8x16、16x16 点阵 ASCII 字模） | `zf_common_typedef.h` |
| `zf_common_font.c` | 字库数据 | `zf_common_font.h` |
| `zf_common_function.h` | 工具宏与函数（绝对值、限幅、软延时、正弦表、最大公约数） | `zf_common_typedef.h` |
| `zf_common_function.c` | 工具函数实现 | `zf_common_function.h` |
| `zf_common_interrupt.h` | 全局中断使能/禁能、NVIC 中断优先级配置 | `hal_device_registers.h`, `zf_common_typedef.h` |
| `zf_common_interrupt.c` | 中断管理实现 | `zf_common_interrupt.h` |
| `zf_common_debug.h` | 调试打印宏（可通过 UART 输出调试信息） | `zf_common_typedef.h` |
| `zf_common_debug.c` | 调试功能实现（如 printf 重定向） | `zf_common_debug.h`, `zf_driver_uart.h` |

> **关键依赖链：**  
> `zf_common_typedef.h` ← 所有其他文件都依赖此文件  
> `zf_common_headfile.h` ← 用户只需包含此文件即可

---

### 2.3 第③层 — 芯片外设驱动层 (`zf_driver/`)

每个驱动文件均遵循 `zf_driver_xxx.h` + `zf_driver_xxx.c` 配对。

| 文件 | 功能 | 依赖（除 `zf_common_typedef.h` 外） |
|------|------|------|
| `zf_driver_gpio.h/c` | GPIO 输入/输出/复用功能配置、引脚枚举 | `hal_device_registers.h` |
| `zf_driver_uart.h/c` | UART 初始化、发送、接收（中断/DMA） | 需配合 GPIO 初始化引脚复用 |
| `zf_driver_spi.h/c` | SPI 主从模式通信 | 需配合 GPIO 初始化引脚复用 |
| `zf_driver_iic.h/c` | 硬件 I2C 通信 | — |
| `zf_driver_soft_iic.h/c` | 软件模拟 I2C（任意 GPIO 引脚） | `zf_driver_gpio.h` |
| `zf_driver_soft_spi.h/c` | 软件模拟 SPI（任意 GPIO 引脚） | — |
| `zf_driver_adc.h/c` | ADC 单次/连续转换 | — |
| `zf_driver_dac.h/c` | DAC 输出（当前为预留空壳，无实际 API） | — |
| `zf_driver_pwm.h/c` | PWM 波形输出 | — |
| `zf_driver_timer.h/c` | 通用定时器 | — |
| `zf_driver_pit.h/c` | 周期性中断定时器（PIT），基于 TIM1~TIM8 实现 | — |
| `zf_driver_encoder.h/c` | 正交编码器接口 | — |
| `zf_driver_exti.h/c` | 外部中断 EXTI 配置 | `zf_driver_gpio.h` |
| `zf_driver_dma.h/c` | DMA 数据传输 | — |
| `zf_driver_delay.h/c` | 毫秒/微秒级延时（基于 SysTick） | 需 `zf_common_clock.h` 提供时钟频率 |
| `zf_driver_flash.h/c` | 片内 Flash 读写 | — |
| `zf_driver_sdio.h/c` | SDIO 接口（用于 SD 卡） | `hal_device_registers.h` |

> **说明：** 所有 driver 文件都直接或间接依赖 `zf_common_typedef.h`。  
> 部分驱动（如 UART、SPI）的 `c` 文件中会通过 `hal_device_registers.h` 和 HAL 层（如 `hal_uart.h`、`hal_spi.h`）操作寄存器。

---

### 2.4 第④层 — 外接设备驱动层 (`zf_device/`)

本层每个文件对应一种具体的外接模块，依赖底层的 driver 层 API 和 common 层类型定义。

#### 显示类

| 文件 | 驱动设备 | 底层依赖驱动 |
|------|----------|-------------|
| `zf_device_ips114.h/c` | 1.14" IPS LCD（135×240，SPI） | SPI（硬件或软件）、GPIO |
| `zf_device_ips200.h/c` | 2.0" IPS LCD（SPI） | SPI（硬件或软件）、GPIO |
| `zf_device_tft180.h/c` | 1.8" TFT LCD（SPI） | SPI、GPIO |
| `zf_device_oled.h/c` | SSD1306 OLED（SPI） | SPI（软件）、GPIO |

#### 摄像头类

| 文件 | 驱动设备 | 底层依赖驱动 |
|------|----------|-------------|
| `zf_device_mt9v03x.h/c` | MT9V03x 灰度摄像头 | DCMI、GPIO |
| `zf_device_ov7725.h/c` | OV7725 摄像头 | DCMI、SCCB(IIC)、GPIO |
| `zf_device_scc8660.h/c` | SCC8660 摄像头 | DCMI、GPIO |
| `zf_device_tsl1401.h/c` | TSL1401 线性 CCD | ADC、GPIO |
| `zf_device_dl1a.h/c` | DL1A 线性 CCD | IIC（软件或硬件）、GPIO |

#### 传感器（IMU/姿态）

| 文件 | 驱动设备 | 底层依赖驱动 |
|------|----------|-------------|
| `zf_device_icm20602.h/c` | ICM-20602 6轴 IMU | SPI（硬件）或 IIC（软件）、GPIO |
| `zf_device_imu660ra.h/c` | IMU660RA 6轴 IMU | IIC（硬件/软件）、GPIO |
| `zf_device_imu963ra.h/c` | IMU963RA 9轴 IMU | IIC（硬件/软件）、GPIO |
| `zf_device_mpu6050.h/c` | MPU6050 6轴 IMU | IIC（软件）、GPIO |

#### 通信/定位/测距/其他

| 文件 | 驱动设备 | 底层依赖驱动 |
|------|----------|-------------|
| `zf_device_bluetooth_ch9141.h/c` | CH9141 蓝牙模块 | UART |
| `zf_device_wifi_uart.h/c` | WiFi 串口模块 | UART |
| `zf_device_wireless_uart.h/c` | 无线串口模块 | UART |
| `zf_device_gps_tau1201.h/c` | TAU1201 GPS 模块 | UART |
| `zf_device_absolute_encoder.h/c` | 绝对值编码器 | UART 或 SPI |
| `zf_device_key.h/c` | 按键检测 | GPIO |
| `zf_device_sdcard.h/c` | SD 卡读写（SDIO 模式） | SDIO、GPIO |
| `zf_device_type.h/c` | 逐飞科技模块类型自动识别 | UART（自动识别协议） |
| `zf_device_dm1xa.h/c` | DM1TA/DM1RA 超声波测距模块 | TIM（定时器捕获）、GPIO |
| `zf_device_config.h` | 设备配置数据声明（配合 `.lib` 二进制库使用） | — |
| `zf_device_virtual_oscilloscope.h/c` | 虚拟示波器上位机通信 | UART |

---

### 2.5 第⑤层 — 应用组件层 (`components/`)

| 文件 | 功能 | 依赖 |
|------|------|------|
| `components/fatfs/source/ff.h` | FatFs 文件系统 API | `ffconf.h`, `diskio.h` |
| `components/fatfs/source/ff.c` | FatFs 核心实现 | `ff.h` |
| `components/fatfs/source/ffconf.h` | FatFs 配置选项 | 无 |
| `components/fatfs/source/diskio.h` | 底层磁盘 I/O 接口 | 无 |
| `components/fatfs/source/diskio.c` | 磁盘 I/O 实现（需挂载 SD 卡驱动） | `diskio.h`, `zf_device_sdcard.h` |
| `components/fatfs/source/ffsystem.c` | FatFs 操作系统相关接口 | `ff.h` |
| `components/fatfs/source/ffunicode.c` | FatFs Unicode 支持 | `ff.h` |

---

## 三、依赖关系图（Mermaid）

```mermaid
graph TD
    %% 第①层 SDK
    subgraph Layer1["① 芯片 SDK 底层 (sdk/)"]
        HAL[hal_*.c/h<br>HAL Drivers]
        CMSIS[CMSIS Core]
        REG[hal_device_registers.h<br>寄存器定义]
        SYS[system_mm32f3277g.c/h<br>系统初始化]
        STARTUP[startup_mm32f3277g.s<br>启动文件]
        STARTUP --> SYS
        HAL --> REG
    end

    %% 第②层 Common
    subgraph Layer2["② 开源库公共层 (zf_common/)"]
        TYPEDEF[zf_common_typedef.h<br>类型定义 ★基石]
        HEADFILE[zf_common_headfile.h<br>一键包含 ★入口]
        CLOCK[zf_common_clock<br>时钟配置]
        FIFO[zf_common_fifo<br>FIFO缓冲]
        FONT[zf_common_font<br>字库]
        FUNC[zf_common_function<br>工具函数]
        INT[zf_common_interrupt<br>中断管理]
        DEBUG[zf_common_debug<br>调试输出]
        TYPEDEF -->|stdint/stdio等| C_LIB[C标准库]
        CLOCK --> TYPEDEF
        FIFO --> TYPEDEF
        FONT --> TYPEDEF
        FUNC --> TYPEDEF
        INT -->|hal_device_registers.h| REG
        INT --> TYPEDEF
        DEBUG --> TYPEDEF
    end

    %% 第③层 Driver
    subgraph Layer3["③ 芯片外设驱动层 (zf_driver/)"]
        GPIO[zf_driver_gpio]
        UART[zf_driver_uart]
        SPI[zf_driver_spi]
        IIC[zf_driver_iic]
        SOFT_IIC[zf_driver_soft_iic]
        SOFT_SPI[zf_driver_soft_spi]
        ADC[zf_driver_adc]
        DAC[zf_driver_dac]
        PWM[zf_driver_pwm]
        TIMER[zf_driver_timer]
        PIT[zf_driver_pit]
        ENC[zf_driver_encoder]
        EXTI[zf_driver_exti]
        DMA[zf_driver_dma]
        DELAY[zf_driver_delay]
        FLASH[zf_driver_flash]
        SDIO[zf_driver_sdio]
    end

    %% 第③层 → 第①②层依赖
    GPIO --> TYPEDEF
    GPIO --> REG
    UART --> TYPEDEF
    UART -->|运行时| GPIO
    SPI --> TYPEDEF
    SPI -->|运行时| GPIO
    ADC --> TYPEDEF
    DELAY --> TYPEDEF
    DELAY --> CLOCK
    SOFT_IIC --> TYPEDEF
    SOFT_SPI --> TYPEDEF
    SDIO --> TYPEDEF
    DMA --> TYPEDEF

    %% 第④层 Device
    subgraph Layer4["④ 外接设备驱动层 (zf_device/)"]
        IPS114[zf_device_ips114<br>1.14" IPS]
        IPS200[zf_device_ips200<br>2.0" IPS]
        TFT180[zf_device_tft180<br>1.8" TFT]
        OLED[zf_device_oled]
        MT9V03X[zf_device_mt9v03x<br>摄像头]
        OV7725[zf_device_ov7725<br>摄像头]
        SCC8660[zf_device_scc8660<br>摄像头]
        TSL1401[zf_device_tsl1401<br>CCD]
        DL1A[zf_device_dl1a<br>CCD]
        ICM20602[zf_device_icm20602<br>6轴IMU]
        IMU660RA[zf_device_imu660ra<br>6轴IMU]
        IMU963RA[zf_device_imu963ra<br>9轴IMU]
        MPU6050[zf_device_mpu6050<br>6轴IMU]
        SDCARD[zf_device_sdcard<br>SD卡]
        KEY[zf_device_key<br>按键]
        BLUETOOTH[zf_device_bluetooth_ch9141<br>蓝牙]
        WIFI[zf_device_wifi_uart<br>WiFi]
        WIRELESS[zf_device_wireless_uart<br>无线串口]
        GPS[zf_device_gps_tau1201<br>GPS]
        ABS_ENC[zf_device_absolute_encoder<br>绝对值编码器]
        TYPE[zf_device_type<br>模块类型识别]
        DM1XA[zf_device_dm1xa<br>超声波测距]
        VOSC[zf_device_virtual_oscilloscope<br>虚拟示波器]
        CAMERA[zf_device_camera<br>摄像头通用]
        CONFIG[zf_device_config<br>设备配置]
    end

    %% 第④层 → 第③层依赖
    IPS114 --> SPI
    IPS114 --> GPIO
    IPS114 --> TYPEDEF
    IPS200 --> SPI
    IPS200 --> GPIO
    TFT180 --> SPI
    OLED --> SOFT_SPI
    OLED --> GPIO
    ICM20602 -->|可选| SPI
    ICM20602 -->|可选| SOFT_IIC
    ICM20602 --> GPIO
    IMU660RA --> IIC
    IMU660RA --> GPIO
    IMU963RA --> IIC
    MPU6050 --> SOFT_IIC
    SDCARD --> SDIO
    KEY --> GPIO
    BLUETOOTH --> UART
    WIFI --> UART
    WIRELESS --> UART
    GPS --> UART
    ABS_ENC --> UART
    ABS_ENC -->|可选| SPI
    TYPE --> UART
    VOSC --> TYPEDEF
    CAMERA --> FIFO
    CAMERA --> UART

    %% 第⑤层 Component
    subgraph Layer5["⑤ 应用组件层 (components/)"]
        FATFS[FatFs 文件系统]
    end
    FATFS --> SDCARD

    %% 用户代码
    USER[用户应用程序] --> HEADFILE
```

---

## 四、如何使用本库

### 4.1 快速开始

在用户的应用程序中，**只需包含一个头文件**即可：

```c
#include "zf_common_headfile.h"
```

这将自动包含：
- SDK 层全部 HAL 头文件
- Common 层全部工具头文件  
- Driver 层全部外设驱动头文件
- Device 层全部设备驱动头文件
- 组件层 FatFs 头文件

### 4.2 典型初始化流程

```c
#include "zf_common_headfile.h"

int main(void)
{
    // 1. 中断初始化（NVIC 分组配置）
    interrupt_init();

    // 2. 系统时钟配置（推荐 120MHz）
    clock_init(SYSTEM_CLOCK_120M);

    // 3. 外设初始化（按需调用）
    uart_init(UART_1, 115200, UART1_TX_A9, UART1_RX_A10);  // 初始化串口1，指定TX/RX引脚
    gpio_init(A0, GPO, HIGH, GPO_PUSH_PULL);                // 初始化 GPIO A0 推挽输出高电平
    pit_ms_init(TIM6_PIT, 1);                               // PIT 定时器，1ms 周期中断

    // 4. 设备初始化（按需调用）
    ips114_init();                                      // 初始化 IPS114 显示屏

    while(1)
    {
        // 用户主循环
    }
}
```

### 4.3 各层 API 使用示例

#### 公共层工具

```c
// 类型使用
uint8  data8  = 0;
uint16 data16 = 0;
uint32 data32 = 0;

// 工具宏
int32 val = func_abs(-100);         // 绝对值
val = func_limit(500, 200);         // 限幅 [-200, 200]
val = func_limit_ab(500, 0, 300);   // 限幅 [0, 300]

// FIFO 使用
fifo_struct fifo;
uint8 buffer[256];
fifo_init(&fifo, buffer, 256);
fifo_write(&fifo, &byte, 1);
fifo_read(&fifo, &byte, 1);

// 延时
system_delay_ms(1000);   // 延时 1 秒
system_delay_us(100);    // 延时 100 微秒
```

#### 驱动层外设

```c
// --- GPIO ---
gpio_init(A0, GPO, GPIO_LOW, GPO_PUSH_PULL);    // A0 推挽输出
gpio_set(A0);                                   // 输出高
gpio_clear(A0);                                 // 输出低
gpio_toggle(A0);                                // 翻转
uint8 level = gpio_get(A0);                     // 读取输入电平
gpio_init(A1, GPI, GPIO_LOW, GPI_FLOATING_IN); // A1 浮空输入

// --- UART ---
uart_init(UART_1, 115200, UART1_TX_A9, UART1_RX_A10);  // 初始化串口1（需指定 TX/RX 引脚）
uart_write_byte(UART_1, 'A');               // 发送一个字符
uart_write_buffer(UART_1, buffer, len);     // 发送一帧数据
uart_write_string(UART_1, "hello");          // 发送字符串
uint8 ch;
uart_query_byte(UART_1, &ch);              // 查询接收一个字符（返回 1 有数据，0 无数据）

// --- SPI ---
spi_init(SPI_1, SPI_MODE0, SPI_SPEED_MAX, SPI1_SCK_A5, SPI1_MOSI_A7, SPI_MISO_NULL, SPI_CS_NULL);
spi_write_8bit(SPI_1, 0x55);               // 发送并接收一个字节

// --- ADC ---
adc_init(ADC1_CH0_A0, ADC_12BIT);           // 初始化 ADC1 通道0
uint16 adc_val = adc_mean_filter_convert(ADC1_CH0_A0, 10);  // 均值滤波采样 10 次

// --- PWM ---
pwm_init(TIM2_PWM_CH1_A0, 10000, 5000);     // 10kHz A0 引脚输出，占空比 50%（基于 PWM_DUTY_MAX=10000）

// --- DMA ---
dma_set_count(DMA1_CHANNEL1, 100);          // 设置 DMA 传输计数
dma_enable(DMA1_CHANNEL1);                  // 使能 DMA 通道
```

#### 设备层外设

```c
// --- IPS114 / OLED 显示屏 ---
ips114_init();                              // 初始化 IPS114
ips114_clear();                             // 清屏
ips114_show_string(0, 0, "Hello World");    // 显示字符串
ips114_show_int(0, 16, 1234, 4);           // 显示整数
ips114_show_float(0, 32, 3.14f, 4, 2);     // 显示浮点数
ips114_show_binary_image(0, 0, image, w, h, dw, dh);  // 显示二值图像

oled_init();                                // 初始化 OLED
oled_show_string(0, 0, "Hello World");     // OLED 显示字符串
oled_show_printf(0, 2, "val=%d", 100);     // OLED printf 格式化显示

// --- ICM20602 6轴 IMU ---
icm20602_init();                            // 初始化
icm20602_get_accel(&acc_x, &acc_y, &acc_z); // 读取加速度
icm20602_get_gyro(&gyro_x, &gyro_y, &gyro_z);  // 读取陀螺仪

// --- SD 卡 ---
sdcard_init();                              // 初始化 SD 卡
sdcard_read_single_block(block_addr, buffer);  // 读取一个块

// --- 按键 ---
key_init();                                 // 初始化按键
uint8 key_val = key_scan();                 // 扫描按键

// --- 虚拟示波器 ---
virtual_oscilloscope_data_conversion(d1, d2, d3, d4);  // 4通道数据转为发送帧

// --- DL1A 线性 CCD（IIC 接口） ---
dl1a_init();                                // 初始化
uint16 dl1a_adc_val[DL1A_NUM];
dl1a_get_adc(dl1a_adc_val);                 // 获取 CCD 各像素 ADC 值

// --- DM1XA 超声波测距 ---
dm1xa_transmitter_ranging();                 // DM1TA 发射超声波
dm1xa_receiver_ranging(&distance_mm);        // DM1RA 接收并计算距离（mm）
```

#### 文件系统（FatFs）

```c
#include "ff.h"       // 或直接通过 zf_common_headfile.h 包含

FATFS fs;
FIL fil;
FRESULT res;

res = f_mount(&fs, "0:", 1);               // 挂载 SD 卡
res = f_open(&fil, "0:test.txt", FA_WRITE | FA_CREATE_ALWAYS);  // 创建文件
f_printf(&fil, "Hello World\n");           // 写入文件
f_close(&fil);                             // 关闭文件
```

---

### 4.4 文件包含方式建议

| 场景 | 推荐方式 |
|------|---------|
| 快速原型/测试 | 直接包含 `zf_common_headfile.h` |
| 正式项目（优化编译速度） | 按需包含具体头文件 |
| 仅使用 GPIO 和 UART | 包含 `zf_driver_gpio.h` + `zf_driver_uart.h` |
| 使用显示屏 | 包含 `zf_common_typedef.h` + `zf_driver_spi.h` + `zf_device_ips114.h` |
| 使用 SD 卡 + FatFs | 包含 `zf_device_sdcard.h` + `ff.h` |

### 4.5 开发环境配置要点

1. **包含路径设置：**
   - `libraries/sdk/CMSIS/Include`
   - `libraries/sdk/drivers`
   - `libraries/sdk`
   - `libraries/zf_common`
   - `libraries/zf_driver`
   - `libraries/zf_device`
   - `libraries/components/fatfs/source`

2. **启动文件选择：**
   - IAR 环境 → `sdk/iar/startup_mm32f3277g.s`
   - MDK 环境 → `sdk/mdk/startup_mm32f3277g.s`

3. **链接脚本：**
   - IAR 环境 → `sdk/iar/linker/` 下的 .icf 文件
   - MDK 环境 → `sdk/mdk/linker/` 下的 .sct 文件

4. **宏定义：** 根据所选 IDE 定义对应的编译器宏（如 `__ICCARM__` 或 `__MDK__`）

---

## 五、文件总览（快速索引）

| 层级 | 目录 | 文件数 | 说明 |
|------|------|--------|------|
| SDK 底层 | `sdk/CMSIS/Include/` | ~30 | ARM CMSIS 内核头文件 |
| SDK 底层 | `sdk/drivers/` | ~24对 | MCU 外设 HAL 驱动（.c + .h） |
| SDK 底层 | `sdk/` | 6 | 主头文件、系统初始化、寄存器定义 |
| SDK 底层 | `sdk/iar/` | 2 | IAR 启动文件 + 链接脚本 |
| SDK 底层 | `sdk/mdk/` | 2 | MDK 启动文件 + 链接脚本 |
| 公共层 | `zf_common/` | 6对 .h/.c + 2个纯头文件 | 类型定义/时钟/FIFO/字库/函数/中断/调试/入口头文件 |
| 驱动层 | `zf_driver/` | 17对 | GPIO/UART/SPI/IIC/ADC/PWM/定时器/DMA 等外设驱动 |
| 设备层 | `zf_device/` | ~25对 | 各种外接模块驱动（含 DM1XA 超声波模块） |
| 组件层 | `components/fatfs/source/` | 7 | FatFs 文件系统源码 |

---

*本文档由逐飞科技 MM32F327X_G8P 开源库自动分析生成，如有遗漏欢迎补充。*
