#ifndef _my_image_show_h_
#define _my_image_show_h_

#include "zf_common_headfile.h"

//-------------------------------------------------------------------------------------------------------------------
// 图像采集 + 大津法二值化 + IPS200 显示 + 巡线流水线 + 巡线绘制
// 应在主循环中每帧调用一次。
//
// 调用后，巡线结果可通过 my_line_follow.h 中的全局变量获取：
//   Left[], Right[], Mid[]  — 边线/中线数据
//   Dir_err                  — 方向控制误差
//   imgTop                   — 截止行
//-------------------------------------------------------------------------------------------------------------------
extern void image_show (void);

#endif
